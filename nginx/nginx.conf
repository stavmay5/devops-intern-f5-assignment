events {}

http {
    server {
        listen 8080;
        location / {
            root /var/www/html;
            index index.html;
        }
    }

    server {
        listen 8081;
        location / {
            return 500 "Simulation of Internal Server Error";
        }
    }
}